name: client

description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  ########################################################
  # MELOS PACKAGES 

  # THEME
  theme: any

  # CORE 
  core: any

  # UI-KIT
  ui_kit: any

  # END MELOS PACKAGES 
  ########################################################

  # Add the dotenv package to load environment variables from a .env file.
  # This is useful for managing sensitive information like API keys.
  flutter_dotenv: ^5.2.1
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true